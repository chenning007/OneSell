# Required for Electron — ensures node_modules are hoisted so require("electron")
# resolves to the built-in Electron API in the main process (not the npm path stub).
node-linker=hoisted
shamefully-hoist=true
