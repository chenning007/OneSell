/**
 * Responsive layout configuration (P8: breakpoints in config, not hardcoded).
 */

/** Viewport width (px) at which grids collapse to 2 columns */
export const BREAKPOINT_COLLAPSE = 800;

/** Compact padding (px) applied below BREAKPOINT_COLLAPSE */
export const PADDING_COMPACT = 16;

/** Default padding (px) applied at or above BREAKPOINT_COLLAPSE */
export const PADDING_DEFAULT = 40;
