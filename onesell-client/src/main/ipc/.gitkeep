// IPC handler registry — Main Process
// SECURITY: every handler validates incoming payload with Zod before processing.
// Unknown message shapes are rejected. Credentials are never a valid message type.
// Implemented in M1. See docs/ARCHITECTURE.md §4.4
export {};
